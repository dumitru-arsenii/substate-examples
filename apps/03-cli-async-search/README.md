# 03 CLI Async Search

Use Substate in a CLI when asynchronous work should expose a latest result and derived status.

```bash
npm run start --workspace apps/03-cli-async-search -- react
npm run start --workspace apps/03-cli-async-search -- fail
```

This example simulates a remote search so it can run offline.

