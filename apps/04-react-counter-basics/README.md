# 04 React Counter Basics

Use `@substate/react` when components should read and run typed Substate flows through a provider.

```bash
npm run dev --workspace apps/04-react-counter-basics
```

This example demonstrates one mutation, one derived selector, and the hook status object.

