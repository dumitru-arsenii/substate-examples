# 05 React Derived Cart

Use Substate when UI totals should be derived from source cart state instead of manually synchronized after every button click.

```bash
npm run dev --workspace apps/05-react-derived-cart
```

This example stores cart lines with one mutation and derives subtotal, tax, and total through selectors.

