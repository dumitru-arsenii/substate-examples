# 06 React Live Feed

Use Substate when an observable stream should become typed application state that React can read through the same hook shape as selectors and mutations.

```bash
npm run dev --workspace apps/06-react-live-feed
```

This example uses RxJS to simulate a live feed. Changing channels switches the subscription arguments.

